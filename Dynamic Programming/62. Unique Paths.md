
# 62. Unique Paths

There is a robot on an m x n grid. The robot is initially located at the top-left corner (i.e., grid[0][0]). The robot tries to move to the bottom-right corner (i.e., grid[m - 1][n - 1]). The robot can only move either down or right at any point in time.

Given the two integers m and n, return the number of possible unique paths that the robot can take to reach the bottom-right corner.

The test cases are generated so that the answer will be less than or equal to 2 * 109.

##Java


<!-- Brute Force :
class Solution {
    public int uniquePaths(int m, int n) {
        return totalWays (0,0,m,n);
    }
    public int totalWays (int currentRow, int currentColm, int m, int n) {
        // m-1 and n-1 are the target
        if (currentRow > m -1 || currentColm > n - 1)
        return 0;

        if (currentRow == m -1 && currentColm == n -1)
        return 1;

        int rightway = totalWays(currentRow, currentColm +1,m,n);
        int downway = totalWays(currentRow +1,currentColm,m,n);
        return rightway + downway;
    }
} -->
 
class Solution {
    public int uniquePaths(int m, int n) {
        return totalWays (0,0,m,n, new HashMap <String,Integer>());
    }
    public int totalWays (int currentRow, int currentColm, int m, int n, HashMap <String,Integer> memo) {
        // m-1 and n-1 are the target
        if (currentRow > m  || currentColm > n )
        return 0;

        if (currentRow == m -1 && currentColm == n -1)
        return 1;

         String currentKey = Integer.toString(currentRow) + "_" + Integer.toString(currentColm);
          
        if(memo.containsKey(currentKey))
            return memo.get(currentKey);

        int rightway = totalWays(currentRow, currentColm + 1,m,n, memo);

        int downway = totalWays(currentRow + 1,currentColm,m,n, memo);

        memo.put(currentKey,(rightway + downway));
        
        return memo.get(currentKey);
    }
}
## Problem Link

https://leetcode.com/problems/unique-paths/

